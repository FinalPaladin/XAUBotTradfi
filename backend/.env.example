# Application
APP_NAME=XAUBot TradFi
DEBUG=false

# SQLite (relative to backend/)
DATABASE_URL=sqlite:///./data/xau_bot.db

# CORS (comma-separated origins for React UI later)
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
